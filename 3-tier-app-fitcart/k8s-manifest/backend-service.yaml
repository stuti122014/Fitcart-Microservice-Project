apiVersion: v1
kind: Service
metadata:
  name: fitcart-backend-service
spec:
  selector:
    app: fitcart-backend

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

  type: ClusterIP